package Practice;

import java.util.ArrayList;
import java.util.List;

public class ArrayPractice {
	/*
	 * 
	 * public static List<Description> main(String[] args) { // TODO Auto-generated
	 * method stub String input = "01|sankar$02|siva$03|deva$04|haree";
	 * List<Description> all = new ArrayList<>(); String[] keyvalue =
	 * input.split("\\$");
	 * 
	 * List<String> siva = new ArrayList<>(); for (int k = 0; k < keyvalue.length;
	 * k++) { siva.add(keyvalue[k]); }
	 * 
	 * List<String> descvalue = new ArrayList<>(); List<String> descvalue2 = new
	 * ArrayList<>(); // System.out.println(keyvalue.length); for (int i = 0; i <
	 * keyvalue.length; i++) { String[] aa = keyvalue[i].split("\\|"); for (int j =
	 * 0; j < aa.length; j++) { Description desc = new Description();
	 * descvalue.add(aa[j]); j++; // desc.setDesc(descvalue[j]);
	 * descvalue2.add(aa[j]); }
	 * 
	 * } // all.add(descvalue)
	 * 
	 * // Description sadds=new Description(); // for(int m=0;m<keyvalue.length;m++)
	 * { // String dood; // String s=Integer.toString(m); // dood="dsd"+s; //
	 * descvalue[m].toString(); // // sadds.setCd(input);
	 * 
	 * // }
	 * 
	 * for (String b : descvalue) { System.out.println(b); } for (String c :
	 * descvalue2) { System.out.println(c); }
	 * 
	 * for (String a : siva) { System.out.println(a); }
	 * 
	 * }
	 * 
	 */}
